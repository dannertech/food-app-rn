import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const FilteredSection = (props) => {
    return(
        <View>
            <Text>Title</Text>
            <Text>Filtered Section</Text>
        </View>
    )
};

const styles = StyleSheet.create({});

export default FilteredSection;